FROM node:18-alpine

WORKDIR /app

# Copy only needed files for MCP server
COPY backend/package.json backend/package-lock.json ./
COPY backend/src ./src

# Install dependencies
RUN npm ci

# Expose port
EXPOSE 3000

# Start MCP server
CMD ["node", "src/tradingview-mcp/server.js"]
